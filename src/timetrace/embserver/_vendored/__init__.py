"""Vendored third-party code (Apache 2.0). See LICENSE in this directory.

`qwen3_vl_embedding.Qwen3VLEmbedder` is the official Qwen3-VL-Embedding inference
wrapper, vendored so the embedding server is self-contained at runtime.
"""
